region: us-east-1

function_name: i92_freshen
handler: service.handler
role: lambda_api
description: AWS Lambda that freshens i92 data source when new CSV available in i92 S3 bucket

# if access key and secret are left blank, boto will use the credentials
# defined in the [default] section of ~/.aws/credentials.
#aws_access_key_id: 
#aws_secret_access_key: 

# dist_directory: dist
timeout: 300
memory_size: 128
